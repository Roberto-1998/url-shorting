import React from 'react';
import LinkForm from '../LinkForm/LinkForm';
import { ShortenBox } from './ShortenContainer.styles';

const ShortenContainer = () => {
  return (
    <ShortenBox>
      <LinkForm />
    </ShortenBox>
  );
};

export default ShortenContainer;
